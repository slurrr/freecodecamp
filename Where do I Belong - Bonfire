function where(arr, num) {
  arr.sort();
  for (var i = 0; i < arr.length; i++) {
    if (arr[i] >= num)
      return arr.indexOf(arr[i]); 
  } 
  return arr.length;
}

where([10, 20, 30, 40, 50], 35, "");
